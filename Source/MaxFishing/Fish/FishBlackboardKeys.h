// Copyright MaxFishing Project

#pragma once

#include "CoreMinimal.h"

namespace FishBlackboard
{
	const FName FishState(TEXT("FishState"));
	const FName LureLocation(TEXT("LureLocation"));
	const FName PlayerLocation(TEXT("PlayerLocation"));
}
