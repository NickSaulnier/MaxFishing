// Copyright MaxFishing Project

#pragma once

#include "CoreMinimal.h"

class UWorld;

/** Spawns a school of AAIFishCharacter in the runtime lake (tagged lake preferred). Server / standalone only. */
MAXFISHING_API void MaxFishingSpawnDemoTroutInLake(UWorld* World);
